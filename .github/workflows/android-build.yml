name: Android Build Worker

on:
  repository_dispatch:
    types: [trigger-build]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build with Gradle
        run: |
          echo "Building APK for ${{ github.event.client_payload.url }}"
          # In a real workflow, you would use a template project (like Capacitor or WebView wrapper)
          # and inject the assets (logo, url, packageId) before building
          # ./gradlew assembleRelease

      - name: Sign APK
        run: echo "Signing with Private Keys..."

      - name: Upload to Firestore/Storage
        run: |
          echo "Uploading ${{ github.event.client_payload.buildId }}.apk"
          # curl -X POST ...
